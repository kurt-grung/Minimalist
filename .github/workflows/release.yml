name: Release

on:
  release:
    types: [created]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., 1.0.0)'
        required: true
        type: string

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'
          cache-dependency-path: cms/package-lock.json
      
      - name: Install dependencies
        working-directory: ./cms
        run: npm ci
      
      - name: Run tests
        working-directory: ./cms
        run: npm test
      
      - name: Build package
        working-directory: ./cms
        run: npm run build
      
      - name: Check package contents
        working-directory: ./cms
        run: |
          echo "Checking build artifacts..."
          ls -la dist/
          test -f dist/index.js && echo "✓ dist/index.js exists"
          test -f dist/index.d.ts && echo "✓ dist/index.d.ts exists"
          test -f dist/cli.js && echo "✓ dist/cli.js exists"
          echo "All build artifacts verified"

  # Note: Publishing to npm should be done manually or with proper authentication
  # Uncomment and configure if you want automated publishing
  # publish:
  #   name: Publish to npm
  #   needs: build-and-test
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'release'
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20.x'
  #         registry-url: 'https://registry.npmjs.org'
  #     
  #     - name: Install dependencies
  #       working-directory: ./cms
  #       run: npm ci
  #     
  #     - name: Publish to npm
  #       working-directory: ./cms
  #       run: npm publish
  #       env:
  #         NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

